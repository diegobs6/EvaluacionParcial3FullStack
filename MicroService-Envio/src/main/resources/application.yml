spring:
  application:
    name: MicroService-Envio

  datasource:
    url: jdbc:mysql://localhost:3306/db_envio
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect

  flyway:
    url: jdbc:mysql://reseau.proxy.rlwy.net:23104/railway?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    user: root
    password: UFJdIPqRoWnbMyDRHkrkjgvrIhgYsnij
    ignore-migration-patterns: "*:*"
    baseline-on-migrate: true
    enabled: true

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_URL:http://localhost:8761/eureka/}

server:
  port: 8088

springdoc:
  api-docs:
    enabled: true
  swagger-ui:
    enabled: true
    path: /envio/swagger-ui.html

usuario:
  service:
    url: https://evaluacionparcial3fullstack-production.up.railway.app/v1/usuarios

pedido:
  service:
    url: http://localhost:8086/v1/pedidos