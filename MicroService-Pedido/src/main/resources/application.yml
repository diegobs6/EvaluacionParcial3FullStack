spring:
  application:
    name: MicroService-Pedido

  datasource:
    url: jdbc:mysql://thomas.proxy.rlwy.net:35855/railway
    username: root
    password: xIbrJlrixuUvFYxVcBRhvtIJFNjtPBLW
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect

  flyway:
    enabled: true
    ignore-migration-patterns: "*:*"
    baseline-on-migrate: true

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_URL:http://localhost:8761/eureka/}
    register-with-eureka: true
    fetch-registry: true

server:
  port: 8086

springdoc:
  api-docs:
    enabled: true
  swagger-ui:
    enabled: true
    path: /pedido/swagger-ui.html

usuario:
  service:
    url: https://evaluacionparcial3fullstack-production.up.railway.app/v1/usuarios
