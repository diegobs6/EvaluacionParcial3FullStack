spring:
  application:
    name: MicroService-Pedido

  datasource:
    url: jdbc:mysql://localhost:3306/db_pedido
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect

  flyway:
    ignore-migration-patterns: "*:*"
    baseline-on-migrate: true
    enabled: true

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_URL:http://localhost:8761/eureka/}
    register-with-eureka: true
    fetch-registry: true

server:
  port: 8086

springdoc:
  api-docs:
    enabled: true
  swagger-ui:
    enabled: true
    path: /pedido/swagger-ui.html

usuario:
  service:
    url: http://localhost:8081/v1/usuarios
